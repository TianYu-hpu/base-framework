
server:
  port: 8080

spring:
  profiles:
    active: dev

urlSuffix: .html
web:
  staticFile: .css,.js,.png,.jpg,.gif,.jpeg,.bmp,.ico,.swf,.psd,.htc,.htm,.html,.crx,.xpi,.exe,.ipa,.apk

session:
  sessionTimeout: 1800000
  sessionTimeoutClean: 120000
cookie:
  name: base.framework.id